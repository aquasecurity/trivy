apiVersion: v1
kind: Pod
metadata:
  name: limited-pod
  namespace: limitedns
spec:
  containers:
  - name: my-alpine-sleeper
    image: alpine:3.17.1
    command: [ "/bin/sh", "-c", "--" ]
    args: [ "while true; do sleep 30; done;"]