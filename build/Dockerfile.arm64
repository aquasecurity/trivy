FROM arm64v8/alpine:3.17.2

# Add user and group
ARG user=appuser
ARG group=appgroup
ARG uid=1000
ARG gid=1000

RUN addgroup -g ${gid} ${group}
RUN adduser -u ${uid} -G ${group} -h /home/${user} -D ${user}

# Switch to user
USER ${uid}:${gid}
WORKDIR /home/${user}

COPY trivy /
